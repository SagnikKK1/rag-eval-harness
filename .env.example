# Copy to .env and fill in. .env is gitignored.

# Anthropic — used by the generator (and, in Part 2, the Ragas judge).
# Retrieval works WITHOUT this key; only generation needs it.
ANTHROPIC_API_KEY=

# Optional alternative generation provider (kept pluggable; not required).
OPENROUTER_API_KEY=
